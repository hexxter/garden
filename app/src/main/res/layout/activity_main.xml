<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:ems="10"
        android:id="@+id/editText_min"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="34dp"
        android:layout_alignParentStart="true"
        android:text="120" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="min"
        android:id="@+id/textView"
        android:layout_alignTop="@+id/editText_min"
        android:layout_alignBottom="@+id/editText_min"
        android:layout_toEndOf="@+id/editText_min"
        android:textSize="@dimen/abc_text_size_large_material" />

    <ToggleButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="light"
        android:id="@+id/toggleButton_light"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="65dp"
        android:textOff="light off"
        android:textOn="light on"
        android:textSize="@dimen/abc_text_size_large_material" />

    <ToggleButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="socket"
        android:id="@+id/toggleButton_socket"
        android:layout_marginTop="49dp"
        android:layout_below="@+id/toggleButton_light"
        android:layout_alignParentStart="true"
        android:textOff="socket off"
        android:textOn="socket on"
        android:textSize="@dimen/abc_text_size_large_material" />

</RelativeLayout>
